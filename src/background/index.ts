/**
 * @file Background entrypoint. Placeholder until the toggle logic task:
 * real initialization (settings, injecting into already open Gmail tabs)
 * arrives in follow-up tasks.
 */

chrome.runtime.onInstalled.addListener(() => {
    // No-op placeholder: keeps the background entry alive and the manifest valid.
});
