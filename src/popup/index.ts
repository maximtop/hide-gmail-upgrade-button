/**
 * @file Popup entrypoint. Renders a placeholder until the toggle task adds
 * the real single-switch UI.
 */

const app = document.getElementById('app');

if (app) {
    app.textContent = 'Hello';
}
